== README

This is a demo Discussions app.

Download the project file or clone it and then follow these steps to run this application:
	rake db:create
	rake db:migrate
	rails s -e production (for Rails 4)
	rackup private_pub.ru -s thin -E production
 
